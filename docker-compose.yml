services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: kanban
      POSTGRES_USER: kanban
      POSTGRES_PASSWORD: kanban_dev_only
    ports:
      - "5432:5432"
    volumes:
      - kanban_pg_data:/var/lib/postgresql/data

volumes:
  kanban_pg_data:
